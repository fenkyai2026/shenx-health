{
  "name": "shenx-health",
  "version": "1.0.0",
  "description": "SHENX HEALTH — aplikasi kesehatan harian (kalori, BMI, latihan jalan & pernapasan)",
  "main": "index.js",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:release": "cap sync android && cd android && ./gradlew assembleRelease",
    "serve": "npx --yes http-server www -p 5173 -c-1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor-community/text-to-speech": "^8.0.2",
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/geolocation": "^8.2.2"
  },
  "private": true
}
